import styled from 'react-emotion';

const ListColumnHeader = styled('div')`
  font-size: 12px;
  text-transform: uppercase;
  font-weight: bold;
  color: ${p => p.theme.gray3};
`;

export default ListColumnHeader;
